// Load in our dependencies
// const {expect} = require('chai');

// Define our tests
describe('A PostApp component', function () {
  describe('with no content', function () {
    it('renders empty content', function () {
      if (2 !== 2) { throw new Error('hi'); }
    });
  });
});
